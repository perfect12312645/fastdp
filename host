# FastDP 主机清单配置文件示例
# 格式说明：[主机组名] 定义主机组，下方为主机列表

# [all] 是默认主机组，包含所有主机
# 主机配置格式：IP/域名 [参数=值 参数=值 ...]
# 支持参数：
#   user      - 登录用户名（默认：root）
#   port      - SSH端口（默认：22）
#   password  - 登录密码（如不指定，需提前设置SSH免密登录）

# ============================================================
# 主机区间展开（支持 [start:end:step]，零填充自动识别）
#   node-[100:105]   → node-100 ~ node-105
#   node-[100:105:2] → node-100, node-102, node-104（步长2）
#   node-[01:04]     → node-01 ~ node-04（零填充）
#   [192.168.10.100:101] 或 192.168.10.[100:101] → IP 段展开
#   host 文件内使用无需加引号（命令行参数使用需加引号，避免被 shell glob 解析）
#
# 参数覆盖：区间展开的所有主机共享同一行参数；
#   个别主机参数不同时，单独写一行放在后面覆盖（后声明优先）：
#   node-[100:105] user=root password=common
#   node-103 password=special        # 只这一台密码不同
#
# 注意：命令行参数中写区间（如 fastdp shell -a 'uptime' 'node-[100:105]'）
#   展开后的主机同样必须已在此文件中声明（含连接参数）。
# ============================================================

# 可以定义多个主机组，按业务或环境分组
# 注意：如果主机在不同组中重复出现，只会执行一次（后声明覆盖参数）

# [cpu]
# 192.168.1.106 user=root port=22 password=123456
# 192.168.1.107 user=root port=22
# 192.168.1.108
# 192.168.1.109 user=root

# [gpu]
# 192.168.1.110